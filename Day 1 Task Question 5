#python 3.7.1

for i in range (1,4):
  runs = int(input("run scored by the player in 60 balls:"))
  strk_rate = (runs / 60) * 100
  ex_runs = int((strk_rate / 100) * 120)
  max_six = runs // 6
  print ("strike rate ",strk_rate)
  print ("extra runs",ex_runs)
  print ("Maximum sixes",max_six)
